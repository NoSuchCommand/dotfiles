# ~/.gitconfig
[color]
	ui = true
[alias]
	lol  = log --oneline --all --graph --decorate
	glog = log --oneline --all --graph --no-decorate
	slog = log --oneline --all --decorate
	ol   = log --oneline --no-decorate
	what = !gitwhat
	co   = checkout
[push]
	default = current
[merge]
	tool = vimdiff
	conflictstyle = diff3
[mergetool]
	prompt = false
[diff]
	tool = vimdiff
	wordRegex = .
[difftool]
	prompt = false
[status]
	showUntrackedFiles = all
[pull]
	rebase = preserve
